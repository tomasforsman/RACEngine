[window]
# Override the window title (default: your assembly name)
title = My Sample Game

# Override the initial window size (width,height). Default: 1280,720
size = 1280,720

# Enable or disable VSync (true/false). Default: true
vsync = false

# You can add other settings here as you extend WindowSettings:
# position = 100,50
# windowState = Maximized
# windowBorder = Fixed
